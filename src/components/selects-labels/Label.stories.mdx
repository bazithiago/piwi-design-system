import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Label, labelContent } from './Label.jsx'

<Meta title="Selects and Labels/Label" component={Label} />

# Label

<Canvas>
  <Story
    name="Label"
    args={{
        ...Label.defaultProps,
        label: 'desligados',
        number: 2,
        isSelected: false,
    }}
  >
    {(args) => <Label {...args}>{args.isSelected && labelContent(args.label, 'x')}</Label>}
  </Story>
</Canvas>