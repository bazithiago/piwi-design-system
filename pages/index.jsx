import styled from 'styled-components';

const HomepageStyles = styled.div`
    color: red;
`

export default function Homepage() {
    return(
        <HomepageStyles>
            Olá mundo!
        </HomepageStyles>
    )
}